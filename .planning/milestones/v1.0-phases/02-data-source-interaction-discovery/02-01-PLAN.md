---
phase: 02-data-source-interaction-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/data-source/binance-vision.types.ts
  - src/data-source/binance-vision.service.ts
  - tests/data-source/binance-vision.service.test.ts
autonomous: true
requirements:
  - FR1
  - FR4

must_haves:
  truths:
    - "Given a symbol and interval, the tool outputs a list of valid `binance.vision` `.zip` file URLs."
    - "The output list does not contain any `.zip.CHECKSUM` entries."
    - "The tool handles cases where no `.zip` files are found (e.g., empty list output)."
    - "The tool reports an error if the `binance.vision` URL is inaccessible or returns a non-200 status."
  artifacts:
    - path: "src/data-source/binance-vision.service.ts"
      provides: "Function to discover kline data .zip file URLs"
      min_lines: 50
    - path: "src/data-source/binance-vision.types.ts"
      provides: "Type definitions for binance vision data"
      min_lines: 5
    - path: "tests/data-source/binance-vision.service.test.ts"
      provides: "Unit tests for binance-vision.service.ts"
      min_lines: 30
  key_links:
    - from: "src/data-source/binance-vision.service.ts"
      to: "node-fetch"
      via: "HTTP requests to binance.vision"
      pattern: "import.*node-fetch"
    - from: "src/data-source/binance-vision.service.ts"
      to: "cheerio"
      via: "HTML parsing"
      pattern: "import.*cheerio"
    - from: "src/data-source/binance-vision.service.ts"
      to: "binance.vision API"
      via: "external HTTP calls"
      pattern: "fetch\\('https://data\\.binance\\.vision.*'"
---

<objective>
Implement the core functionality to discover downloadable kline data `.zip` files for a given symbol and interval from `binance.vision`.

Purpose: This plan establishes the capability for the tool to interact with `binance.vision`, fetch HTML directory listings, and extract relevant `.zip` file URLs, forming the foundation for subsequent download phases.
Output: A `binance-vision.service.ts` module capable of returning a filtered list of kline data `.zip` file URLs, along with comprehensive unit tests.
</objective>

<execution_context>
@/home/sea212/.gemini/get-shit-done/workflows/execute-plan.md
@/home/sea212/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-source-interaction-discovery/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From Node.js built-in `url` module:
```typescript
class URL {
  constructor(url: string, base?: string | URL);
  toString(): string;
  // ... other properties/methods
}
```

From `02-RESEARCH.md` (proposed `getKlineZipFileUrls` function):
```typescript
/**
 * Fetches HTML from a URL, parses it, and extracts valid .zip file URLs.
 * @param discoveryUrl The base URL of the binance.vision directory listing.
 * @returns A promise that resolves to an array of full .zip file URLs.
 */
async function getKlineZipFileUrls(discoveryUrl: string): Promise<string[]> { /* ... */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dependencies and Define `binance-vision` Service Interface</name>
  <files>
    package.json
    package-lock.json
    src/data-source/binance-vision.types.ts
    src/data-source/binance-vision.service.ts
  </files>
  <action>
    1. **Install `node-fetch` and `cheerio`**: Add `node-fetch` (v3.x) and `cheerio` (v1.x) to `package.json` as production dependencies and run `npm install`.
    2. **Create `src/data-source/binance-vision.types.ts`**: Define a simple interface for `KlineDiscoveryOptions` (e.g., `{ symbol: string; interval: string; market?: 'spot' | 'futures/um' | 'futures/cm'; dataType?: 'monthly' | 'daily'; }`) and `KlineFileUrlResult` (e.g., `string[]`).
    3. **Create `src/data-source/binance-vision.service.ts`**:
       - Import necessary modules (`node-fetch`, `cheerio`).
       - Export an async function `getKlineZipFileUrls(options: KlineDiscoveryOptions): Promise<string[]>` with the function signature and an initial skeleton.
       - Implement the URL construction logic based on the `KlineDiscoveryOptions` provided, using the pattern from `02-RESEARCH.md` (e.g., `https://data.binance.vision/data/${options.market}/${options.dataType}/klines/${options.symbol.toUpperCase()}/${options.interval}/`). Use default values for `market` ('spot') and `dataType` ('monthly') if not provided.
       - Initially, the function can just return `[]` or log the constructed URL. The actual fetching/parsing will happen in Task 2.
  </action>
  <verify>
    <automated>
      npm install && grep -q "node-fetch" package.json && grep -q "cheerio" package.json && test -f src/data-source/binance-vision.types.ts && test -f src/data-source/binance-vision.service.ts && grep -q "export async function getKlineZipFileUrls" src/data-source/binance-vision.service.ts
    </automated>
  </verify>
  <done>
    `node-fetch` and `cheerio` are installed, `binance-vision.types.ts` contains type definitions, and `binance-vision.service.ts` has the `getKlineZipFileUrls` function signature with initial URL construction logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Core Kline Data Discovery Logic with Unit Tests</name>
  <files>
    src/data-source/binance-vision.service.ts
    tests/data-source/binance-vision.service.test.ts
  </files>
  <action>
    1. **Implement `getKlineZipFileUrls` in `src/data-source/binance-vision.service.ts`**:
       - Complete the implementation of the `getKlineZipFileUrls` function using `node-fetch` to fetch HTML, `cheerio` to parse it, and `URL` for robust URL resolution, as detailed in the "Fetch and Parse HTML" and "Fetching HTML and Extracting .zip Links" sections of `02-RESEARCH.md`.
       - Ensure robust error handling for HTTP responses (checking `response.ok`).
       - Filter out links that do not end with `.zip` or end with `.zip.CHECKSUM`.
       - Use `new URL(href, discoveryUrl)` to construct full URLs.
    2. **Create Unit Tests in `tests/data-source/binance-vision.service.test.ts`**:
       - Create a new test file for `binance-vision.service.ts`.
       - Use a mocking library (e.g., `jest-fetch-mock` if Jest is set up, or a simple mock for `node-fetch`) to simulate HTTP responses.
       - Write at least three test cases:
         - **Successful discovery**: Mock a `binance.vision` HTML response containing several `.zip` and `.zip.CHECKSUM` links. Assert that `getKlineZipFileUrls` returns only the valid `.zip` URLs.
         - **No `.zip` files**: Mock an HTML response with no `.zip` links. Assert that an empty array is returned.
         - **HTTP error**: Mock a non-200 HTTP response (e.g., 404). Assert that the function handles the error gracefully (e.g., returns an empty array or throws a specific error if designed to).
       - Ensure the tests cover the filtering logic for `.zip.CHECKSUM` files.
  </action>
  <verify>
    <automated>
      npm test tests/data-source/binance-vision.service.test.ts
    </automated>
  </verify>
  <done>
    The `getKlineZipFileUrls` function in `src/data-source/binance-vision.service.ts` is fully implemented and passes all unit tests in `tests/data-source/binance-vision.service.test.ts`, successfully demonstrating the ability to discover and filter kline `.zip` files from mock HTML.
  </done>
</task>

</tasks>

<verification>
To verify this phase, manually run the discovery service with a known symbol and interval (e.g., BTCUSDT, 1m) and observe the console output.
Expected output: A list of full URLs to `.zip` files, with no `.zip.CHECKSUM` entries.
```bash
# Example manual verification command (assuming a CLI command is added in a future phase)
# node src/cli.js discover --symbol BTCUSDT --interval 1m
# For now, focus on the unit tests passed
npm test tests/data-source/binance-vision.service.test.ts
```
</verification>

<success_criteria>
- The `getKlineZipFileUrls` function in `src/data-source/binance-vision.service.ts` correctly constructs URLs for `binance.vision` based on symbol and interval.
- The function successfully fetches and parses HTML content from `binance.vision` (verified via mock responses in tests).
- The function accurately extracts links ending with `.zip` and filters out those ending with `.zip.CHECKSUM`.
- All unit tests in `tests/data-source/binance-vision.service.test.ts` pass, demonstrating the reliability of the discovery logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-source-interaction-discovery/02-01-SUMMARY.md`
</output>
