---
phase: 04-data-extraction-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/services/extraction.service.ts, tests/services/extraction.service.test.ts]
autonomous: true
requirements: [FR6]

must_haves:
  truths:
    - "ExtractionService successfully extracts a .zip file to a target directory"
    - "ExtractionService deletes the source .zip file after successful extraction"
    - "ExtractionService handles extraction errors, preserves the .zip file, and cleans up partial files if extraction fails"
  artifacts:
    - path: "src/services/extraction.service.ts"
      provides: "Zip extraction and cleanup logic"
    - path: "tests/services/extraction.service.test.ts"
      provides: "Unit and integration tests for ExtractionService"
  key_links:
    - from: "src/services/extraction.service.ts"
      to: "extract-zip"
      via: "npm package import"
    - from: "src/services/extraction.service.ts"
      to: "node:fs/promises"
      via: "unlink for cleanup"
---

<objective>
Implement the `ExtractionService` for extracting CSV files from downloaded .zip archives and cleaning up temporary files.

Purpose: Provide a robust and reusable extraction mechanism that ensures data integrity and manages storage efficiently.
Output: `ExtractionService` class and its comprehensive test suite (unit + integration).
</objective>

<execution_context>
@/home/sea212/.gemini/get-shit-done/workflows/execute-plan.md
@/home/sea212/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-extraction-finalization/04-RESEARCH.md
@src/services/download.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ExtractionService with Rollback</name>
  <files>package.json, src/services/extraction.service.ts</files>
  <action>
    1. Install `extract-zip` as a production dependency: `npm install extract-zip`.
    2. Create `src/services/extraction.service.ts` and implement a static method `extractAndCleanup(zipPath: string, destDir: string): Promise<void>`.
    3. Use `extract-zip` for the extraction process.
    4. After successful extraction, use `node:fs/promises.unlink` to delete the original `zipPath`.
    5. Implement robust error handling:
       - Wrap extraction in a try-catch block.
       - If extraction fails:
         - Identify the expected CSV filename (matches the zip filename base).
         - Use `fs.access` to check if the partial CSV file exists in `destDir`.
         - If it exists, use `fs.unlink` to delete the partial file.
         - Do NOT delete the source `.zip` file.
         - Throw a descriptive error.
  </action>
  <verify>
    <automated>test -f src/services/extraction.service.ts && grep "extract-zip" package.json</automated>
  </verify>
  <done>
    `ExtractionService` is implemented with extraction, cleanup, and partial-file rollback logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Unit and Integration Tests</name>
  <files>tests/services/extraction.service.test.ts</files>
  <action>
    1. Create `tests/services/extraction.service.test.ts`.
    2. Implement unit tests using Jest, mocking `extract-zip` and `node:fs/promises`.
    3. Test cases for unit tests:
       - Successful extraction and cleanup.
       - Extraction failure: verify partial file cleanup attempt and preservation of zip.
    4. Implement an **integration test**:
       - Programmatically create a small, valid ZIP file in a temporary test directory.
       - Call `ExtractionService.extractAndCleanup`.
       - Verify the CSV is extracted correctly to the destination.
       - Verify the original ZIP is removed.
    5. Implement an **integration test for failure**:
       - Use a corrupted ZIP file.
       - Verify `extractAndCleanup` throws.
       - Verify no partial files are left in the destination.
       - Verify the corrupted ZIP file is still present.
  </action>
  <verify>
    <automated>npm test tests/services/extraction.service.test.ts</automated>
  </verify>
  <done>
    Both unit and integration tests for `ExtractionService` pass.
  </done>
</task>

</tasks>

<verification>
<automated>npm test tests/services/extraction.service.test.ts</automated>
</verification>

<success_criteria>
`ExtractionService` correctly extracts files, cleans up the source archive, and rolls back partial extractions on failure, as verified by automated unit and integration tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-extraction-finalization/04-01-SUMMARY.md`
</output>
