---
phase: 01-project-initialization-basic-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.js
  - package.json
autonomous: true
requirements:
  - FR2
  - NFR3

must_haves:
  truths:
    - "The application can be invoked from the command line."
    - "The `fetch` command can be invoked."
    - "The `--output-dir` option is recognized by the `fetch` command."
  artifacts:
    - path: "src/cli.js"
      provides: "CLI entry point, Commander.js setup"
      min_lines: 20
      contains:
        - "#!/usr/bin/env node"
        - "new Command()"
        - ".command('fetch')"
        - ".option('-o, --output-dir <path>'"
    - path: "package.json"
      provides: "executable `bin` entry"
      contains:
        - ""bin": { "kline-fetcher": "src/cli.js" }"
  key_links:
    - from: "package.json"
      to: "src/cli.js"
      via: "`bin` entry"
      pattern: ""kline-fetcher": "src/cli.js""
---

<objective>
Establish the foundational CLI structure for the 'kline-fetcher' tool using Commander.js. This includes creating the main entry point and defining a basic 'fetch' command that recognizes an output directory option.

Purpose: This plan lays the groundwork for user interaction with the tool, making it runnable from the command line and capable of receiving initial parameters.
Output: A runnable `kline-fetcher` command that accepts a `--output-dir` option.
</objective>

<execution_context>
@/home/sea212/.gemini/get-shit-done/workflows/execute-plan.md
@/home/sea212/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-initialization-basic-cli/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Create CLI entry point and basic command</name>
  <files>
    src/cli.js
    package.json
  </files>
  <action>
    1. Create the `src` directory if it doesn't exist.
    2. Create `src/cli.js` with the `#!/usr/bin/env node` shebang at the top.
    3. Initialize Commander.js, setting the program name (`kline-fetcher`), description, and version (`0.0.1`).
    4. Define a command named 'fetch'.
    5. Add an option to the 'fetch' command: `-o, --output-dir <path>` with a default value of `./data` and a description 'Specify the output directory for data storage'.
    6. For now, the action callback for the 'fetch' command should only log a message indicating it's starting and what the output directory option received is.
    7. Parse the command-line arguments.
    8. Update `package.json` by adding a `"bin"` entry to make `src/cli.js` executable globally as `kline-fetcher`. This will involve adding:
      ```json
      "bin": {
        "kline-fetcher": "src/cli.js"
      }
      ```
    9. After modifying `package.json`, ensure to run `npm install` to link the binary, or instruct the user to do so in the verification.
  </action>
  <verify>
    <automated>
      npm install
      node src/cli.js fetch --help
      kline-fetcher fetch --output-dir /tmp/mydata
    </automated>
    Check that `node src/cli.js fetch --help` displays the help message including the `--output-dir` option.
    Check that `kline-fetcher fetch --output-dir /tmp/mydata` executes, logs 'Starting CLI' and 'Output Directory: /tmp/mydata'.
  </verify>
  <done>
    `src/cli.js` is created as the main executable CLI entry point, `package.json` is updated with the `bin` entry, and the `kline-fetcher fetch --output-dir` command correctly parses the output directory argument.
  </done>
</task>

</tasks>

<verification>
After running `npm install`, confirm that both `node src/cli.js fetch --output-dir /path/to/data` and `kline-fetcher fetch --output-dir /path/to/data` produce console output indicating the CLI started and the correct output directory was recognized.
</verification>

<success_criteria>
The `kline-fetcher` command is installable and executable from the terminal.
The `fetch` subcommand exists and correctly recognizes the `--output-dir` option with its provided value.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-initialization-basic-cli/01-01-SUMMARY.md`
</output>
