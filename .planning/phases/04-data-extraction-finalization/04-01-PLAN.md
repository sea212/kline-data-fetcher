---
phase: 04-data-extraction-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/services/extraction.service.ts, tests/services/extraction.service.test.ts]
autonomous: true
requirements: [FR6]

must_haves:
  truths:
    - "ExtractionService successfully extracts a .zip file to a target directory"
    - "ExtractionService deletes the source .zip file after successful extraction"
    - "ExtractionService handles extraction errors and preserves the .zip file if extraction fails"
  artifacts:
    - path: "src/services/extraction.service.ts"
      provides: "Zip extraction and cleanup logic"
    - path: "tests/services/extraction.service.test.ts"
      provides: "Unit tests for ExtractionService"
  key_links:
    - from: "src/services/extraction.service.ts"
      to: "extract-zip"
      via: "npm package import"
    - from: "src/services/extraction.service.ts"
      to: "node:fs/promises"
      via: "unlink for cleanup"
---

<objective>
Implement the `ExtractionService` for extracting CSV files from downloaded .zip archives and cleaning up temporary files.

Purpose: Provide a robust and reusable extraction mechanism that ensures data integrity and manages storage efficiently.
Output: `ExtractionService` class and its comprehensive unit test suite.
</objective>

<execution_context>
@/home/sea212/.gemini/get-shit-done/workflows/execute-plan.md
@/home/sea212/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-extraction-finalization/04-RESEARCH.md
@src/services/download.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ExtractionService</name>
  <files>package.json, src/services/extraction.service.ts</files>
  <action>
    1. Install `extract-zip` as a production dependency: `npm install extract-zip`.
    2. Create `src/services/extraction.service.ts` and implement a static method `extractAndCleanup(zipPath: string, destDir: string): Promise<void>`.
    3. Use `extract-zip` for the extraction process.
    4. After successful extraction, use `node:fs/promises.unlink` to delete the original `zipPath`.
    5. Implement robust error handling: if extraction fails, the .zip file should NOT be deleted, and a descriptive error should be thrown.
  </action>
  <verify>
    Check if `src/services/extraction.service.ts` exists and `package.json` contains `extract-zip`.
  </verify>
  <done>
    `ExtractionService` is implemented with proper error handling and dependency management.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Unit Tests for ExtractionService</name>
  <files>tests/services/extraction.service.test.ts</files>
  <action>
    1. Create `tests/services/extraction.service.test.ts`.
    2. Implement tests using Jest. Mock `extract-zip` and `node:fs/promises`.
    3. Test cases:
       - Successful extraction and cleanup (verify `extract-zip` called, `unlink` called).
       - Extraction failure (verify `extract-zip` throws, `unlink` is NOT called).
       - Verify proper target directory is passed to `extract-zip`.
  </action>
  <verify>
    Run tests: `npm test tests/services/extraction.service.test.ts`
  </verify>
  <done>
    All unit tests for `ExtractionService` pass with high coverage.
  </done>
</task>

</tasks>

<verification>
`npm test tests/services/extraction.service.test.ts`
</verification>

<success_criteria>
`ExtractionService` correctly extracts files and cleans up the source archive, as verified by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-extraction-finalization/04-01-SUMMARY.md`
</output>
