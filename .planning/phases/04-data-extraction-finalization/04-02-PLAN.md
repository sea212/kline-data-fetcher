---
phase: 04-data-extraction-finalization
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/cli.js]
autonomous: false
requirements: [FR6]

must_haves:
  truths:
    - "CLI successfully downloads and then extracts each .zip file"
    - "Final .csv files are placed in <outputDir>/<SYMBOL>/"
    - ".tmp/ directory is cleared of .zip files after successful extraction"
    - "CLI logs 'Extracted [filename]' for each successful extraction"
  artifacts:
    - path: "src/cli.js"
      provides: "End-to-end download and extraction flow"
  key_links:
    - from: "src/cli.js"
      to: "src/services/extraction.service.ts"
      via: "method call within fetch loop"
---

<objective>
Integrate the `ExtractionService` into the CLI `fetch` command to complete the full data acquisition lifecycle: discovery -> download -> extraction -> cleanup.

Purpose: Automate the final step of data preparation for the user.
Output: Updated `src/cli.js` and a verified functional flow.
</objective>

<execution_context>
@/home/sea212/.gemini/get-shit-done/workflows/execute-plan.md
@/home/sea212/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-extraction-finalization/04-01-SUMMARY.md
@src/cli.js
@src/services/extraction.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ExtractionService into CLI fetch</name>
  <files>src/cli.js</files>
  <action>
    1. Import `ExtractionService` in `src/cli.js`.
    2. Within the `for...of` loop in the `fetch` command, after `DownloadService.downloadFile` succeeds:
       - Define the extraction destination as `symbolDir` (which is the parent of `tempDir`).
       - Call `ExtractionService.extractAndCleanup(destPath, symbolDir)`.
       - Add a log message: `Successfully extracted ${fileName} to ${symbolDir}.`.
    3. Ensure appropriate error handling for the extraction step within the loop (log error, but continue with next file).
  </action>
  <verify>
    Check if `src/cli.js` imports and calls `ExtractionService`.
  </verify>
  <done>
    `ExtractionService` is integrated into the CLI `fetch` loop.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Verification</name>
  <what-built>End-to-end data fetcher with extraction</what-built>
  <how-to-verify>
    1. Run the tool to fetch data for a small symbol/interval, for example:
       `node src/cli.js fetch -s BTCUSDT -i 1m --dataType monthly -o ./uat-data/phase4`
    2. Observe the logs to ensure downloads and extractions occur.
    3. Verify the final structure:
       - `.csv` files should exist in `./uat-data/phase4/BTCUSDT/`.
       - No `.zip` files should remain in `./uat-data/phase4/BTCUSDT/.tmp/`.
  </how-to-verify>
  <action>Perform a manual fetch and verify the resulting file structure.</action>
  <verify>Approved by user</verify>
  <done>Full flow verified on real data</done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual verification of files on disk.
</verification>

<success_criteria>
The tool successfully performs the full discovery-download-extraction-cleanup cycle, resulting in organized .csv files in the target directory.
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-extraction-finalization/04-02-SUMMARY.md`
</output>
